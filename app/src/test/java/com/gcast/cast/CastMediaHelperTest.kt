package com.gcast.cast

import org.junit.Test
import org.junit.Assert.*

class CastMediaHelperTest {

    @Test
    fun `CastMediaHelper object exists and is accessible`() {
        // Simple test to verify the helper object is properly defined
        assertNotNull("CastMediaHelper should not be null", CastMediaHelper)
    }
}